package com.example.mindchess.audio_processing

import android.util.Log

private var LOG_TAG = "HotSifSpottingService"

class HotSifSpottingService {

    fun predict(buffer: FloatArray) {
        Log.v(LOG_TAG, "Yeah that was definitely a word, maybe probably I dunno what do you want from me?")
    }

    // This is the AI stuff placeholder


}